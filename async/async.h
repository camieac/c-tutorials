#ifndef ASYNC_H
#define ASYNC_H

struct thread_args {

	// Mutex to data file being read and written in an overlapping time period

	// File descriptors so that threads can access the same file


};

#endif //ASYNC_H
